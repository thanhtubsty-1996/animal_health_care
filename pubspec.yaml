name: animal_health_app
description: A new Flutter project.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # UI & Theming
  cupertino_icons: ^1.0.6
  flex_color_scheme: ^8.0.0
  google_fonts: ^6.2.1
  fl_chart: ^1.0.0

  # State Management
  flutter_bloc: ^9.0.0
  bloc: ^9.0.0
  
  # Routing
  go_router: ^16.0.0

  # Dependency Injection
  get_it: ^8.0.0
  injectable: ^2.4.1

  # HTTP Client
  dio: ^5.4.3+1
  
  # Utilities
  intl: ^0.20.0
  equatable: ^2.0.5
  provider: ^6.1.2
  animations: ^2.0.11
  meta: ^1.12.0 # Added meta

  # Data
  faker: ^2.1.0 # Added faker

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  flutter_lints: ^4.0.0
  build_runner: ^2.4.9
  injectable_generator: ^2.6.1

flutter:
  uses-material-design: true
